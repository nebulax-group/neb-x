"""Compatibility imports for paths now defined in common.config."""

from .config import (
    DATA_DIR as DATA_DIR,
    DATA_DIRS as DATA_DIRS,
    DOCS_DIR as DOCS_DIR,
    EXAMPLE_PREDICTION_PATHS as EXAMPLE_PREDICTION_PATHS,
    INFO_KIT_PATHS as INFO_KIT_PATHS,
    LABEL_PATHS as LABEL_PATHS,
    LOGS_DIR as LOGS_DIR,
    MODEL_DIRS as MODEL_DIRS,
    MODELS_DIR as MODELS_DIR,
    OUTPUTS_DIR as OUTPUTS_DIR,
    PLOTS_DIR as PLOTS_DIR,
    PREDICTION_FILENAMES as PREDICTION_FILENAMES,
    PREDICTION_PATHS as PREDICTION_PATHS,
    PREDICTIONS_ARCHIVE_NAME as PREDICTIONS_ARCHIVE_NAME,
    PREDICTIONS_DIR as PREDICTIONS_DIR,
    PREDICTIONS_ZIP_PATH as PREDICTIONS_ZIP_PATH,
    REFERENCE_DIR as REFERENCE_DIR,
    REPO_ROOT as REPO_ROOT,
    SCRIPTS_DIR as SCRIPTS_DIR,
    SRC_DIR as SRC_DIR,
    SUBMISSION_DIR as SUBMISSION_DIR,
    SUBMISSION_FORMAT_DIR as SUBMISSION_FORMAT_DIR,
    SUBSYSTEMS as SUBSYSTEMS,
    TEST_PATHS as TEST_PATHS,
    TRAIN_PATHS as TRAIN_PATHS,
)
