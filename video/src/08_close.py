"""What it is for, and where to find it. ~15 s.

The predictive-maintenance case in one line, then the repository and prototype
URLs, held on screen long enough to read.
"""

from manim import Scene


class Close(Scene):
    def construct(self) -> None:
        pass
