"""The held-out scores. ~20 s.

Per subsystem, then the Overall, which divides by four whether or not a
subsystem was attempted.
"""

from manim import Scene


class Results(Scene):
    def construct(self) -> None:
        pass
