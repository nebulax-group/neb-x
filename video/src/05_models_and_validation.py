"""How the split was designed, and what it scored. ~25 s.

Grouped so that no recording appears on both sides of it, cross-validated
rather than one lucky run. Methodological soundness is graded alongside the
metric, so this scene is not optional.
"""

from manim import Scene


class ModelsAndValidation(Scene):
    def construct(self) -> None:
        pass
