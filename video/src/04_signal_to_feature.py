"""One idea per subsystem, taken from the signal itself. ~30 s.

The silences between door cycles, ACV cabin temperature minus its own setpoint,
rail vibration bands, SHM rainflow counting. The scene that shows the physics
was read rather than a window handed to a model.
"""

from manim import Scene


class SignalToFeature(Scene):
    def construct(self) -> None:
        pass
