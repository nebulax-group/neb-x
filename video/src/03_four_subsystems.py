"""The four subsystems as four shapes of problem. ~25 s.

Door segment detection, ACV ranking, rail classification, SHM regression, each
with the metric it is scored on. Establishes that all four were attempted,
which is what the Overall Score rewards.
"""

from manim import Scene


class FourSubsystems(Scene):
    def construct(self) -> None:
        pass
