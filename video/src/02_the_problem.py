"""What the raw data is, and who has to act on it. ~20 s.

Four unlabelled sensor streams off a moving train, and the two readers the
organisers name: an operator making a split-second judgement, and an engineer
new to the parameters.
"""

from manim import Scene


class TheProblem(Scene):
    def construct(self) -> None:
        pass
