# Pinned so all three machines resolve identically. Verified on Python 3.14.7 (macOS).
# Regenerate with: pip freeze > requirements.txt

numpy==2.5.3
scipy==1.18.1
pandas==3.0.6
openpyxl==3.1.5          # ACV cases are .xlsx; without this nothing loads
scikit-learn==1.9.1
streamlit==1.64.0        # the compulsory app

# lightgbm is deliberately absent: its wheel needs libomp, which macOS does not ship,
# so it installs but fails to import. sklearn's HistGradientBoostingClassifier is the
# same algorithm with no native dependency. Not worth a broken teammate at hour seven.
